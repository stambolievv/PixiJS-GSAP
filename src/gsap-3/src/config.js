const config = {
  selection: [0, 0.1, 0.2, 0.5, 1, 2],
  dots: {
    length: 10,
    jump: 50,
    color: {
      from: '#a7ce01',
      to: '#ffa500'
    }
  }
};

export default config;